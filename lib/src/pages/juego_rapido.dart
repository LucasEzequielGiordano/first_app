import 'package:flutter/material.dart';

class JuegoRapido extends StatelessWidget {
  const JuegoRapido({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
